# 🌗 道中の一期一会 ＋ 実況の可読性 指示書（Opus 5 向け・2026-08-01）

> **方向の決定はこの文書が正本**（Fable 5 が決裁済みの方針に基づき決定）。実装は Opus 5。
> 状態の正本＝`docs/STATUS_LEDGER.md`。関連の正本＝`docs/PACING_DESIGN_RESEARCH.md` v2・
> `docs/GAME_EXPERIENCE_DESIGN.md`（発見マトリクス 告知7:余白3）。

---

## 決定した方向（なぜこれか）

**長い区間の答えは「残り％」ではなく「道中で出会うもの」**（ユーザー決裁・[[propose-fun-not-instruments]]）。
そして実測の結果、この島は**既に「時間帯」を知っている**：

- `KONRON_SPOTS` の62スポットに `time:` メタが入っている（夜10・昼11・夕系10・早朝3…）。
  **書かれているのに、ゲーム側は一度も使っていない。**
- 歩ける島には きらめき（`KW_FINDS`）・環境文・NPC・時間帯パレット5種が既にあるが、
  **どれも時間で変わらない**＝何時に歩いても同じ島。

よって方向は1つ：**「来る時間で、島が違う」を実装する。**
夕暮れにしか現れない磯、夜だけの猫の集会、未明の出漁。プレイヤーは「あの場所は夕方に
行くと何かあるらしい」を覚え、長い中盤に**戻る理由**が生まれる。
これは理論側とも一致する（D1=転はイベント層に置く／どうぶつの森の「日ごとの小さな変化」が
リテンションを作る側であって、ローンのメーター側ではない／発見マトリクスの「余白3」の充填）。

### ❌ この指示書で作ってはいけないもの（全部ユーザーNO済み）
- 進捗バー・「あと◯%」・残数表示・先月比などの**計器**。1つでも足したらこの作業は失敗。
- 章解放条件の変更（動詞の門）・資産関門の変更・レース数値（着順/オッズ/配当/FinalPower）への干渉。
- 実況への同着ネタの復活（撤去済み・live 283a3c9）。
- 「穴狙いが速い」非対称の手当て（ユーザー決裁＝直さない・触れない）。

## §0 絶対規律
`docs/PACING_EXECUTION_DIRECTIVE.md` §0 の8条をそのまま適用（実体を見る／実機DOM・canvas実測／
両ブランチ／perl／resetGame／文字スキャン／終わったら閉じる）。加えて：
- 声表厳守：**ポロは言葉を持たない**（しぐさのみ）。ミミは講釈しない（反応・驚き・質問）。
  未登場キャストは名前も出さない（fail-closed）。
- 検証でスクショが撮れないとき＝`_shot`サーバ方式。★描画は `kwUpdate(1/60, S)` を1回呼んでから
  `kwDraw`（bgFar→world→fg）。update を飛ばすと **S.camera に sc/vw/vh が入らず背景が写らない**（実測）。
- 時間帯のテストは `window.kwNow` を上書きして固定 → **終わったら必ず戻す**（`location.reload()` が確実）。

---

# Part A 島の一期一会（時と場の出会い）

## A1 時間帯の語彙
歩ける島の時間帯は `kwNow().k` ＝ **未明／朝／昼／夕暮れ／宵／夜** の6種（`_kmIslandNow` 連動・実時計）。
スポットの `time:` 文字列は表記ゆれが23種あるので**機械変換しない**。A2/A3 では各項目に
**このバケット名を明示的に書く**（誤変換の温床を作らない）。

## A2 時間限定の入口（3つ・すべて既存スポット＋既存写真）

**仕組み**：入口定義に `when: ["夕暮れ","宵"]` を追加し、`kwDoorVisible(d)` で
`d.when && !d.when.includes(kwNow().k)` なら**存在ごと出さない**。
- ★`KM_HIDDEN` を**使わない**こと。あれはピン式地図のスポット可視も束ねているので、
  時間条件を入れると**ピン地図側のスポットまで時間で消える**（副作用）。歩き専用の `when` を新設する。
- ★道標の煙（❓）と同様、描画側（`kwDrawDoors` 相当）も `kwDoorVisible` を通っているか grep で確認。
  通っていなければ通す（入口だけ消えて道標が残る、を防ぐ）。

| 場所 | 入口 | 時間帯 | 中身 | 事前確認 |
|---|---|---|---|---|
| beach | 🌇「夕焼けの磯」 | 夕暮れ | `kwShoot("bangara")`（バンガラ溶岩海岸・time:"夕方"） | 写真 `images/konron/spots/bangara.webp` 実在確認済み |
| city | 🌫「朝霧の桟橋」 | 朝・未明 | `kwShoot("kirimina")`（霧港） | **写真の実在を grep で確認してから**結線（無ければ黒画面撮影の罠） |
| town | 🍸「ドラゴンベル・ラウンジ」 | 宵・夜 | `kwShoot("lounge")`（屋上バー・time:"夕〜夜"） | ★lounge は tier2＝資産ゲートあり。`when` に加えて **`_kmSpotOpen(KONRON_SPOTS.lounge)` も AND** すること（歩きがピン地図の関門を抜け道にしない） |

- 置くセルは**歩けるマスに隣接**させ、`tools/check_walk.mjs` が全緑のままであること
  （ハーネスは when を知らないので対象に数える＝到達可能な場所に置けば通る）。
- 撮影はオーバーレイ＝**`stay: true` 必須**（シーン破壊の既知バグ）。
- hint の文言で時間を予告しない（「夕方に来い」と書かない）。**居合わせた人だけが見つける**。
  告知側は既に旅ノートのスポット情報（time欄）が担っている＝告知7:余白3 の役割分担どおり。

## A3 一期一会の出来事（12本以上）

**仕組み**：新テーブル `KW_MOMENTS`（scene_konron.js 内・データ駆動）。
```js
// { area, times: ["夜"], kind: "spark"|"ambient", text: "…", poro: true?, once: "_kw_m_<id>"? }
```
- `spark`＝既存の「きらめき」枠に**時間条件つきで混ぜる**（配置ロジックは既存の KW_FINDS と同じ流れ。
  近づくと✨→トースト）。`ambient`＝画面上部の環境文の候補に**その時間帯だけ**追加。
- `once` 指定があるものだけ `setStoryFlag` で一度きり。それ以外は**繰り返し出てよい**
  （季節の風物は毎回あって自然。一度きりにするのは「事件」だけ）。
- ★`poro: true` は `poroFound()` かつ随行中のみ。**ポロに台詞を書かない**（しぐさの描写のみ）。

**初期セット（このまま実装してよい・声はミミの一人称観察）**：

| area | 時間 | kind | 文 |
|---|---|---|---|
| city | 夜 | spark | 🐈 路地のすみに、猫が三匹まるく座ってる。……会議中みたい。じゃまはやめよう。 |
| city | 朝 | ambient | 市場のセリの声。数字が飛びかって、ちょっとレース場みたい。 |
| race | 朝 | spark | 🐾 まだ湿ってる、大きな足あと。今朝、だれかここを走ったんだ。 |
| race | 夜 | ambient | だれもいない走路を、風だけが走っていく。 |
| onsen | 宵 | ambient | いま、灯籠にぜんぶ火が入った。……宵だ。 |
| onsen | 未明 | spark | ♨️ 湯気がまっすぐ立ってる。今日の一番湯、まだだれも入ってない。 |
| beach | 夜 | spark | 🌊 波打ちぎわが、ぼんやり光ってる。……夜光虫だ。 |
| beach | 夕暮れ | ambient | 浜がぜんぶ、橙いろ。 |
| fishing | 未明 | ambient | 船が一列に出ていく。灯りが海にならんでる。 |
| fishing | 昼 | spark | 🎶 網直し場から鼻歌。手は止まってないのに、ずっと同じ節。 |
| falls | 朝 | spark | 🌈 しぶきの虹が、朝は二重にかかってる。（★NPCの「朝いちばんが濃い」と呼応） |
| falls | 夜 | ambient | 滝の音のむこうに、星。水と光が、おなじ方向に流れてる。 |
| town | 宵 | ambient | 通りのランプが順番に灯っていく。点けてまわる人の足音。 |
| どこでも | — | spark(poro) | 🥹 ポロが立ち止まって、砂をほじってる。……貝がら。くれるらしい。（once） |

- 追加してよいが、**1エリア1時間帯に1本まで**（出会いは薄くまくから効く。密度を上げすぎると環境音になる）。

## A4 NPCの言葉が時間で変わる

**仕組み**：NPC定義の `line`（固定1本）を後方互換のまま拡張：
```js
{ c, r, s, line: "従来の一言",            // fallback（必須のまま残す）
  linesByTime: { "朝": "…", "夜": "…" } } // 任意。kwNow().k が一致すればこちらを優先
```
- `kwAct` の NPC 分岐で `n.linesByTime && n.linesByTime[kwNow().k] || n.line` に差し替えるだけ。
- **全13人に最低1つ**、時間帯の別台詞を書く（2つ以上あるとなお良い）。声のルール：
  村人はその土地の生活の言葉。**レースの数字・賭けの助言はさせない**（顧問の領分）。
- 例（このまま使ってよい）：
  - city の羊角の老人（s:0）朝：「朝の船はいいよ。積んでくるのは魚だけじゃない、今日のうわさもだ。」
  - onsen の女将（s:5）夜：「夜のお湯はね、長湯していいの。だれも急かさないから。」
  - beach の子ども（s:3）夕暮れ：「見て、太陽が海にじゅっ、て入るんだよ。音はしないけど、ぜったい じゅっ、なの。」
  - fishing の網直し（s:5）未明：「見送りかい。……ここの朝は、船が出てからが長いんだ。」

## A5 検証（Part A）
1. `node --check` → `tools/check.mjs` → `tools/check_walk.mjs`（全緑維持・town含む7エリア）。
2. 時間帯ごとの実測：`window.kwNow` を6種に固定し、
   - A2 の入口が**その時間だけ**現れる（両側＝出る時間/出ない時間を各1回ずつ）
   - A3 の spark/ambient が対応時間帯で出る（該当エリアに入って DOM/トースト実測）
   - A4 の台詞が切り替わる（`kwAct` 実行でトースト文面を読む）
3. lounge 入口が**資産不足なら宵でも出ない**こと（tier ゲートの AND を実測）。
4. `grep -n "％\|%.*残り\|先月比\|進捗"` で**計器を足していない**ことを機械確認。
5. 文字スキャン → `resetGame()` → `KW_V`・`?v=` バンプ → 両ブランチ。
6. **自分で歩く**：夜のcityで猫に会い、夕暮れのbeachで磯に入り、朝のfallsで虹を見る。
   会えたかどうかを完了報告に書く（[[playtest-before-done]]）。

---

# Part B 実況の可読性（忙しすぎて読めない）

## B0 実測済みの構造（この理解を前提に作業する）
- テキストは**3系統が同時に流れている**：
  ①実況（call）と解説（color）＝各「直近2行」を吹き出し表示（`shownLines`・`race_canvas.js`）
  ②視聴者チャット＝間隔 2000ms→終盤1300→900ms（**終盤ほど速いのは設計**・`chatNextAt`）
  ③フロート（「N着」「差せ！」等の画面内ポップ）
- スタート前の間＝入場（`ENTRY_DUR`＝最後の竜の到着+settle・可変）＋カウントダウン `preT = 3.0` 秒。
- 全ログボタンは既にある。決着時の `flushTelop`（決め台詞を言い切る）も既にある＝**触らない**。

## B1 まず計測（作る前に・30分で終わる）
実レース1本を 1× で流し、`shownLines` への push を時刻つきで記録して：
- 実況+解説の**行間隔の分布**（中央値・最短）
- 1行の文字数分布
を出す。**基準：日本語の読速≈500字/分 → 25字の行を読むには約3秒**。
行間隔の中央値が3秒を大きく割っているなら診断確定。数字を本書に記録してから B2 へ。

## B2 決定した方針（3段構え・**レース時間は変えない**）

レース自体を長くする案は**採らない**（テンポが命の画面。読めない解決を全員の待ち時間で払うのは悪手）。
「読む物を減らす・読む時間を作る・読まなくていい物を分ける」の3段で解く：

### B2-1 実況・解説に「最低滞留時間」を保証する
- 新しい行を出すとき、直前の行の表示から **MIN_DWELL（初期値2.6秒・B1の実測で調整）** 経っていなければ
  キューに積む。キューが2本を超えたら**雑談（chatter/topic系）から間引く**。
- **絶対に落とさない核**：スタートの一声・先頭交代・仕掛け（スパート）・決め台詞・写真判定・着順。
  行に優先度フラグが無ければ、`race_broadcast.js` の生成時に `core: true` を付ける（表示専用の付加）。
- 2×/3× 再生時は MIN_DWELL を速度で割る（速度を上げた人は自分で選んでいる）。

### B2-2 「読む物」はスタート前に移す
- `preT` を **3.0 → 5.0 秒**（+2秒だけ。ユーザー案の採用形。スキップは既存のまま効く）。
- 紹介・雑談カテゴリの行を**入場〜カウントダウン帯で消化**する（レース中の総量が減る）。
  入場は元々可変で間延びしないので、ここに1〜2本置くのが自然。

### B2-3 チャットは「読ませない」と割り切る（衝突だけ消す）
- 視聴者チャットは賑わいの表現＝**全部読ませる設計にしない**（頻度・速度は現状維持）。
- ただし**実況の新行が出た瞬間だけ、チャットの次の追加を0.8秒遅らせる**
  （同時に2箇所で文字が動く瞬間を消す＝視線の奪い合いをなくす）。

## B3 実装位置と不可侵
- 触るのは `race_canvas.js` の pumpTelop まわり・`preT` 初期値・`chatNextAt`、
  必要なら `race_broadcast.js` の行生成に `core` フラグ付加。**すべて表示専用**。
- τ の進行・着順・オッズ・配当・`flushTelop`・スキップ・全ログ・速度段は**触らない**。

## B4 受け入れ（Part B）
1. B1 の計測を修正後にもう一度回し、**核行の滞留 ≥ MIN_DWELL（1×）**・**核行のドロップ0** を数字で示す。
2. 雑談の間引き数と、スタート前へ移した行数を報告する（何を犠牲にしたか隠さない）。
3. 決着時の決め台詞・写真判定・着順フロートが従来どおり出る（回帰確認）。
4. **自分で1レース観る**：1×で実況が読み切れるか。読めなければ MIN_DWELL を上げて再計測。
5. 最終判定はユーザーのプレイ（「読みにくい」は体感の言葉なので、数字が合っても体感で直す）。

---

## 完了の定義（両Part共通）
- [ ] Part A：A2×3・A3×12以上・A4×13人ぶんが実機実測済み／計器ゼロを機械確認
- [ ] Part B：B1計測→B2実装→B4再計測の数字が本書に記録されている
- [ ] 両ブランチにデプロイ済み・本書に「✅＋日付＋確認方法」・`STATUS_LEDGER.md` 更新済み
