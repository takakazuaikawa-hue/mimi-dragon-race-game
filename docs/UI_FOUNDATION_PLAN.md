# 🧱 UI土台工事の正本（2026-08-02 開始・第1弾＝角丸 完了）

## 結論：レイアウト・各ページのコンセプトを残したまま統一することは**可能**（実証済み）

ユーザーの問い＝「失敗すると今のデザインが崩壊する大改修。今のビジュアルと各ページの
コンセプトを残したまま統一するのは本当に可能か」。

答え＝可能。ただし条件つき：**「見た目が変わっていないこと」を毎回機械で証明しながら進める**。
第1弾（角丸759箇所中592箇所のトークン化）で方法そのものを実証した：

1. **見た目の指紋**（`tools/ui_snapshot_snippet.js`）＝18画面のDOM全要素について
   デザイン系の計算済みスタイル15種をハッシュ化。工事前に基準を取り、工事後に照合。
2. **A/Aテスト**＝何も変えずに2回測って18/18一致（計測器自体の信頼性を先に確認）。
3. **完全一致の置換だけ**＝値が同じものだけを var() へ寄せる（数学的に見た目が変わらない）。
   値を動かす統一（11px→12px等）は**見た目が変わる別工事**＝ユーザー決裁が要る。
4. 結果＝18/18一致で完了。しかも途中で**本物のバグを1件検出**した：
   既存トークン系（下記）と知らずに衝突し `--r-4` が 4px と 22px の二重定義になり、
   13箇所が22pxに化けた。ゲートが media 画面の差分で捕捉→合流して修正→再度18/18。
   **このゲートが無ければ「崩壊」がまさに起きていた**＝ユーザーの懸念は正しかった。

## 正本のトークン系＝「UI装飾第1波」ブロック（style.css 後方の :root）

2026-08-01 の第1波が既に設計していた系に**合流**する。並行の系を作らない（前科あり）。

| 種類 | トークン | 意味 |
|---|---|---|
| 角丸4段 | `--r-1:9px / --r-2:12px / --r-3:16px / --r-4:22px` | チップ/部品/カード/窓。**これ以外を増やさない** |
| 形状2種 | `--r-full:999px / --r-round:50%` | ピル形・正円（段ではなく形） |
| 移行トークン | `--rd-4/6/8/10/14/18` | 4段の外にある現状値の**仮の住所**。最終的に4段へ寄せて消す（値を動かすのでユーザー決裁つき） |
| 高さ3段 | `--e-1/2/3` | 影の物差し（第1波・既存） |

**各ページのコンセプト（kiko-page の明るいブログ面・yt-page のダーク動画面・ig の黒 等）は
スコープ付きクラスとして正当**。トークンは値を1つも動かしていないので、ページの顔は不変。

## 見張り＝`tools/check_ui_tokens.mjs`（ラチェット）

直書きの数は一方向にしか動けない。天井（実測167）を超えたら赤。減ったら天井を下げて更新。
トークンの**二重定義**も検査する（--r-4事故の再発防止）。

## 無変化トークン化の現在地（2026-08-03 更新）

| フェーズ | 対象 | 結果 | 状態 |
|---|---|---|---|
| 第1弾 角丸 | border-radius 759箇所 | 592統治／167残債 | ✅ 完了（18/18ゲート） |
| 第2弾 色 | 16進（url行・定義行除く）2,432箇所 | 上位17色547統治／1,885残債 | ✅ 完了（17画面が前日基準と一致＋snsは日替わりDOMと無罪証明） |
| 第3弾 余白 | padding 779箇所・288種 | 上位17種242統治／525残債 | ✅ 完了（18/18ゲート） |
| ~~影~~ | box-shadow 371箇所 | **341種＝ほぼ全て一点もの。無変化トークン化のてこ無し**（8回以上の反復は none の13件だけ・実測） | →「値を動かす統一」へ移管 |
| （別工事）値の統一 | 残債と--rd-/--hx-/--pd-を正規スケールへ吸収・影を--e-1/2/3へ | 見た目が**変わる** | 要デモ決裁 |

各フェーズの手順：A/Aテスト→基準取得→完全一致置換のみ→照合→ラチェット較正→デプロイ。
照合に失敗したら**巻き戻す**（失敗状態でのデプロイ禁止）。
★日付の罠：SNSは日替わりでDOM構成が変わる。基準と照合は**同じ日**に行うこと（snippet冒頭に詳細）。

---

# 🏔 商業レベルへの登り（算段・2026-08-03）

リサーチ（DevelopersIOのゲームUI/WebUI比較・Knights Chronicle分解・9-slice・Game UI Database）で
判明した「本作と商業スマホゲームの差」は装飾ではなく**4つの層**。土台（上記）はその受け皿。

## 差分の実測（ゲームUIの物差しで測った本作）
- 静止画面：9画面中6画面で無限アニメ0件（商業は静止画面でも常に何かが息づく）
- 専用意匠の枠：0件（border-imageは全CSSで1箇所。298本のUI器が全部「角丸＋グラデ」＝Web汎用カード）
- 触覚：navigator.vibrate 未使用。効果音は17種で充実
- 祝祭：音はあるが視覚の祝祭（紙吹雪・閃光）が無い

## 段階（各段にゲート。順番は依存順＝前の段が次の受け皿）

| 段 | 中身 | クレジット | ゲート |
|---|---|---|---|
| S0 土台 | 無変化トークン化（角丸/色/余白）＋ラチェット | 0 | ✅ 済（このファイル上半分） |
| S1 影と色の統一 | 371影→--e-3段へ・金系近似色→--gold系へ・端数角丸→4段へ。**初めて見た目が変わる** | 0 | 代表3画面の前後比較デモ→ユーザー決裁→全展開→スナップショットは「意図した差分だけか」の確認に使う |
| S2 意匠フレーム試作 | 9-slice の枠1種を生成し、窓（.navpop）1箇所だけに当てる | 〜16 | 実機でユーザーが「化けた」と感じるか。NOならS2路線ごと撤退（16crで方向の是非が買える） |
| S3 フレーム展開 | 決裁済みの意匠をカード/パネル/ボタン/ナビへ。トークン窓口があるので当て先は少数 | 150〜300 | 画面ごとにデモ決裁・ラチェット維持 |
| S4 生きている画面 | 静止6画面に「息づき」（金の粒子・光のスイープ・呼吸）。CSSのみ・reduced-motion尊重 | 0 | 体感デモ決裁（[[juice-approval-via-live-mock]]の型） |
| S5 祝祭と触覚 | 受取/勝利/授与に紙吹雪・閃光レイヤ＋navigator.vibrate（対応端末のみ） | 0 | 同上。レース数値には一切非干渉 |
| S6 見出し書体 | 日本語見出し用サブセットフォント自前配信（CDN不可のため数十KBに絞る） | 0 | 容量とライセンスの確認が先 |

予算：総額150〜320クレジット（残高620・Nano Banana Pro=2cr/枚実測）。S2の16crが最初の分岐点。

### S1の追補（2026-08-03・実機指摘「上の影に巻き込まれる／文字が読めない」より）
実機で2つの本番バグが見つかり、S1の規則が3つ増えた：
1. **状態ルール（:hover/:active/:focus）は統一の対象外**——「通常＝段1／ホバーで段2」の浮きが
   同じ段に丸められて消えるため（デモも除外済み）。
2. **明るい紙面（kiko/kt/lr ページ）は影3段を淡い暖色で持つ**（スコープ上書き・実装済み）。
   きっかけ＝`.card{box-shadow:var(--e-1)}` が後勝ちで `.card.lr-room` に効き、ダーク用の
   黒75%影がクリーム地に落ちて直下のボタンを飲み込んでいた。段の構造は同じ・濃さだけ紙用。
3. **明るい面の文字は --lt-\* 役割色を使う**——lrank カードがダーク用文字色（淡金 on クリーム
   ＝実測1.2:1）で読めなかったのを --lt-gold(#8a5f10・約5:1) 等で修正。今後、明るい面に
   部品を置くときは --lt-系（ink/sub/mut/cap/gold/jade）から選ぶ。
検証＝18画面ゲートで**assetsだけが変わった**（他17画面は完全一致＝外科的修正の証明）。
ラチェットは私の修正自体を一度捕まえ（新規hex 11個）、トークン化で通した＝仕組みが機能している。

## 正直な天井
これを全部やっても Cygames 級（専任UIアーティスト・Live2D常駐・専用エンジン）にはならない。
到達点は「個人開発の上位・商業と並べて見劣りしない」。それ以上は範囲外と明言しておく。

## 規律（このファイルに違反する変更を入れないこと）
- 並行トークン系の新設禁止。定義は第1波ブロックだけ。
- 「完全一致の置換」と「値を動かす統一」を同じコミットに混ぜない。
- 新規CSSの角丸は var(--r-*) を使う（直書きはラチェットで落ちる）。
