# 📗 登り道設計・実行指示書（Opus 5 向け・2026-08-01）

> 方針の正本＝`docs/PACING_DESIGN_RESEARCH.md` **v2**（D1〜D7）。本書はその実行手順。
> 状態の正本＝`docs/STATUS_LEDGER.md`。**着手前に必ず両方を読むこと。**

---

## §0 絶対規律（全ステップ共通・違反したらその作業は失敗）

1. **レース数値は不可侵**：着順・オッズ・配当・FinalPower・出目に触れる変更は書かない。全部「表示専用」。
2. **門番 fail-closed**：未登場の顧問は喋らない・写らない。`_met(key)`（=advisorMet）を必ず条件に入れる。
3. **文書の「未着手/決裁待ち」を信じない**：着手前に grep・実機・`git rev-parse origin/main:<path>` で実体を見る（このセッションで3回、完了済みを作り直しかけた）。
4. **検証してから完了と言う**：機械検証（`node --check`＋`tools/check.mjs`＋`tools/check_chapters.mjs`）→実機DOM実測→（操作系なら）自分で一周プレイ。スクショは古い描画で止まることがあるので**証拠はDOMとcanvas実測**。
5. **デプロイ**：git plumbing（fetch→`GIT_INDEX_FILE`一時index→hash-object→commit-tree→push）で **main と flying-pixel-dragons の両方**。`?v=` バンプは **perl**（PowerShellは文字化け）。
6. **テスト汚染は `resetGame()` で戻す**（コイン・フラグ・勝利数）。
7. **非日本語文字スキャン**を新規テキスト全ファイルに（ハングル/キリル混入が実際に2回起きた）：
   `perl -CSD -ne 'print "$ARGV:$.: $_" if /[\x{AC00}-\x{D7AF}\x{0400}-\x{04FF}]/' <files>`
8. **終わったら閉じる**：本書の該当節に「✅ 実装済み（日付）＋何を見て確かめたか」を書き、`STATUS_LEDGER.md` を更新。

## §0.5 実装してはいけないもの（撤回済み・復活させない）

- ❌ **「動詞の門」＝章解放条件へのAND追加**（第2話に的中要求・第5話にフォロワー要求 等）。
  v1で提案→**ユーザーの言葉の読み違いと判明し撤回**（D3）。理由：速いプレイヤーに人工ブレーキを作る。
- ❌ **資産関門（100万/1億/10億）の変更**（D2）。骨格はジャンル文法として正しい。
- ❌ 「結果を見せた後に伏せて出し直す」タメ演出（C-1）。実装済みかつ偽のタメ。

---

## STEP 1 再実測：到達分布シミュレーション

**目的**：F8-①（前段の暮らしLv上限=4）後の「各資産区間の所要戦数」を数字で出す。
v1の dead zone 判定（第4→5話）は台詞本数だけの推定なので、これで裏を取ってから密度を配る。

**作るもの**：`tools/sim_progression.mjs`（依存ゼロ・node:vm・既存 `tools/check_chapters.mjs` の作法）

1. **読み込むファイルは grep で確定してから書く**（この一覧を鵜呑みにしない）：
   - 竜：`js/data_dragons.js`・`js/data_dragons_ext.js`
   - レース：`js/data_races.js`（RACES）・`js/race_engine.js`（runRace/generateForm）・`js/odds_engine.js`（simulateMarket）
   - 足りない symbol が出たら **stubせず** grep で定義ファイルを探して足す（エンジンを偽物にしたら計測が無意味）。
   - UI系（saveGame/FieldStats/getStoryFlag 等）だけ最小 stub 可。
2. **プロファイル3種 × 200走以上**：
   - 堅実＝1番人気の単勝・賭金は資産の5%（上限1万）
   - バランス＝2〜3番人気の複勝・10%
   - 大勝狙い＝6番人気以下の単勝・10%
   - 破産時は既存の救済額相当を足す（`calculateRescueCoins` があるならそれを読み込む）。
3. **出力**：閾値 3万 / 100万 / 1億 / 10億 それぞれの到達戦数 p10/p50/p90 の表。
   `docs/PACING_DESIGN_RESEARCH.md` に「実測（日付）」節として貼る。
4. ★罠：オッズは**賭けた時点の値で払い戻す**（本編と同じ）。エンジン関数に一切手を入れない。
   乱数はこのシミュだけの話なので `Math.random` でよい（Workflowスクリプトではないので可）。

**受け入れ**：3プロファイルの表が出て、**最も長い区間が特定**できること。

## STEP 2 登り道設計書（実装しない・設計してユーザー検収へ）

**目的**：「総資産100万をどうやってクリアさせるか」に答える。どうぶつの森のローン4条件
（①目標が常に見える ②稼ぎ方を選べる ③到達の儀式 ④次の段が見える）に照らして書く。

`docs/PACING_DESIGN_RESEARCH.md` §3 の形式で、5区間（〜3千/〜3万/〜100万/〜1億/〜10億）ぶん：

```
【区間 X → Y】所要戦数 p50=◯（STEP1の実測値）
  a. 主な収入源（賭け/村/投資の比率がどう変わるか）
  b. この区間で初めて出会うもの（機能・イベント・台詞）
  c. 「あと幾ら」がどこに見えるか
  d. この区間の「転」（予想を裏切る一撃・1つ）
```

**STEP1で最長だった区間から順に b と d を厚くする**。書き終えたら実装せず、ユーザー検収に出す。

## STEP 3 台詞増量（決裁不要・STEP1/2と並行可）

### 3-1 セレスティアの afterRaceSelect（4〜6本）
- 場所：`js/event_registry.js` 末尾（R4の `x_*` 群の作法をそのまま踏襲）。
- 条件：`once: true` ＋ `requiredFlag: "_chapter_intro_5"` ＋ `test: () => _met("celestia")`。
- speaker は `"celestia"`。**書く前に実機で** `speakerAllowed("celestia")` が true になること、
  立ち絵が出ることを確認（立ち絵は `images/cast/stand/celestia*.webp`＝**default / sad / serious / smile の4種のみ**。
  それ以外の expr を書くと落ちるか無表情になる——「表情sadは存在しない」型の事故が神眼実装で実際にあった。まず実在を見る）。
- 中身の方向：神眼の人が**市井のレースを面白がる**視点（答えは言わない・数値に触れない）。
- ★`vnBudget = 1` の罠：1フックで喋るイベントは**1本だけ**。同点 priority は登録順で先勝ち。
  既存の once イベントが残っていると後回しになるだけなので priority は足さなくてよい（急がせたいものだけ明示）。

### 3-2 マクラ増量（4本→8本目安）
- 同じく afterRaceSelect・`_met("makura")`。配信・見られ方・画になる竜、の視点で。

### 3-3 「転」イベント（各章に1つ・既存の結果に台詞を当てるだけ）
- hook は `afterRaceResult`。**ctx は `{ race, hit, popularityRank, bigLoss }` が既に渡っている**
  （`js/ui_render.js` renderResult 参照）。新しい判定は作らない。
- 例：`popularityRank >= 6`（大穴が勝った回）にミズ「数字は嘘をつかない。でも、全部は言わないの」／
  1番人気が飛んで自分も外した回（`popularityRank > 1 && !hit`）にサケ。
- `once: true`。**afterRaceResult には既存6本がいる**ので、発火競合を §3-1 の罠として意識する。

### 3-4 SNSに足す場合の絶対則
- **`POST_TEMPLATES` に足すな**（`postTemplates()` はどこからも呼ばれない死にデータ。R4で実際に踏んだ）。
  足すのは `SNS_POSTS`。**`timelinePosts()` から辿れること**を実機で確認。
- 文面は「最近／このごろ」の現在進行形（`text` は毎描画評価されるので、過去形＋固有名は
  次の行動で**過去の投稿が書き換わって見える**）。
- 実名を出す投稿は `unlock()` で**名前が引けることまで**確認（穴あき投稿の禁止）。

### 3-5 検証レシピ（3-1〜3-4 共通）
1. `node --check` → `tools/check.mjs` → `tools/check_chapters.mjs`。
2. 実機：`renderRaceDetail(RACES[...])` / `renderResult()` の**実経路**で発火させ、
   `.dlg-name` / `.dlg-text` のDOM実測で台詞と話者を確認（スクショ不可）。
3. 門番の**両側**を見る：`_chapter_intro_5` 無し→出ない、有り→出る。
4. §0-7 の文字スキャン → `resetGame()` → デプロイ（§0-5）→ 本書とSTATUS_LEDGERを閉じる。

---

## 完了の定義

- [ ] STEP1 の表が `PACING_DESIGN_RESEARCH.md` に貼られている（3プロファイル×4閾値）
- [ ] STEP2 の5区間表がユーザー検収に出ている（実装はしていない）
- [ ] STEP3 セレスティア4本以上＋マクラ計8本目安＋転3本以上が実機DOM実測済み
- [ ] 両ブランチにデプロイ済み・本書の各節が「✅＋日付＋確認方法」で閉じられている

---

## §5 実施記録

### 🔺 STEP1 再実測（2026-08-01・**主要な発見あり／到達戦数は未完**）
本物のエンジン（`runRace`/`simulateMarket`/`resolveBet`/`generateForm`）を stub なしで呼んで計測。
結果は `docs/PACING_DESIGN_RESEARCH.md` §5 に記載。要点：

- **序盤ほど賭けで勝てない**：本命の単回収率は R1=31% / R3=14% / R5=73% / R7=77%（複はR7で99%）。
  ランダム成分が大きいのは `race_engine.js` のコメントどおり**意図的な設計**。
- **だから登り道は賭け以外に用意されている**：注目レース達成 `max(300, 到達最高×1.5%)`＝雪だるま、
  救済コイン＝暮らし/名声/村に比例、連勝ボーナス、図鑑コンプ報酬。
- **v2 §2-2 の「稼ぎ方の複線が弱い」判定は、正しいが理由が逆だった**。複線は実在する。**見えていない**だけ。
  → 登り道設計の中心は「新しい収入源を作る」ではなく「**既にある複線を選べる形で見せる**」。
- **未完**：「100万まで何戦」は未計測。上の複線を組み込まないシミュは実態と合わない
  （賭けだけだと peak が1000コインから動かないことを実測）。**次はこれらを組み込んだ到達戦数の計測**。

### ✅ STEP3 台詞増量（2026-08-01・13本）
- **セレスティア 0→5本**（`x_celes_*`・afterRaceSelect・`requiredFlag:"_chapter_intro_5"`＋`_met("celestia")`）。
  「視える人が、視えないことを面白がる」距離感で、**答えは一切言わせない**（神眼は最終決戦の切り札）。
- **マクラ 4→9本**（`x_makura_*`）。うち3本は走数40/70・スマホ購入で開く＝長旅の後半を持たせる。
- **「転」3本**（`x_twist_*`・afterRaceResult）。既に渡っている ctx（`popularityRank`/`hit`/`bigLoss`）だけを見る＝
  **新しい判定を作っていない**。大穴が来た回→ミズ／本命が飛んだ回→サケ／大負け→スミカ。
- **結果**：話者別は サケ29／ミミ24／ミズ13／**マクラ9**／スミカ7／**セレスティア5**。
  「いちばん長い区間がいちばん静か」は解消。

**検証**：門番の三段階を実測（新規=0件通る → 第5話読了+5走=6件 → 75走+スマホ=10件）。
`runEventActions` が speaker=celestia・expr=smile/serious の正しいセリフを返すことを確認。
立ち絵は `celestia_smile/_serious/_sad/celestia` の実在を fetch(HEAD) で確認（`_cut` は無いがチェーンで正しく落ちる）。

**★この作業で指示書 §0-7（文字スキャン）が実際に効いた**：書いた台詞に
キリル文字が2箇所（`техника` / `здесь`）混入していたのを機械検出して修正。
あわせて台詞内の Markdown 強調 `**` 3箇所も除去（VNは素のテキスト表示）。
