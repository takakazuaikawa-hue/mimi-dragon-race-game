# 生成×3D×RPG化 統括指示書（正本・2026-07-26）

> **目的**：Higgsfield MCP（クレジット1,200・Plus）を使い、①モールダンジョンの3D化＋キャラ配置（必須）
> ②島めぐりを「歩いて探索するスーファミRPG品質」へ ③ミニアニメ・3D生成物で全体の説得力を上げる。
> **実装担当**：Opus 5（新セッション・1フェーズ1セッション目安）。**このファイルだけで作業完結すること。**
> 親＝docs/MINIGAME_LEVELUP_DIRECTIVE.md（M0〜T3・K1は実装済み）・GAME_EXPERIENCE_DESIGN.md（傘）。
>
> **絶対規律は MINIGAME_LEVELUP_DIRECTIVE §0 の8条をそのまま適用**（レース数値不可侵／通貨不増／門番／
> spec#30／文章規律／perl+両ブランチデプロイ／検証してから完了と言う／スコープ規律）。
> ★特に第7条：**本書の前身セッションで「実装した」と偽る幻覚事故が実際に起きた**。編集のたびに
> grep でファイル実体を確認し、コミット後に git log を確認してから完了と言うこと。

---

## 0. 検証済みの土台（2026-07-26 実測・このまま信じてよい）

### Higgsfield MCP の実測
- `balance` → **credits: 1200**（月次回復済み）。
- **アップロード経路は実証済み**：`media_upload`（presigned URL発行）→ curl PUT（HTTP 200）→
  `media_confirm` → media_id 取得。ミミ立ち絵 `images/cast/mimi/mimi_buniqro_default.png` を
  media_id `befe7523-eccd-44aa-a721-4ab0694bc0c4` としてアップロード済み（再利用可）。
- **モデルカタログ（models_explore で確認済み）**：
  - `nano_banana_pro`（画像・最高品質・1k/2k/4k・image-to-image可）／`seedream_v4_5`（4K精密編集）
  - `image_to_3d`（Meshy）：**単画像→GLB。texture/PBR/rig/animation（clip 0-696）**。
    ※過去の試合3D化で実績あり（[[match-3d-players-pivot]]・baseball_pitching=393・品質良好）
  - `multi_image_to_3d`（2-4視点で精度向上）／`3d_rigging`（既存GLBのリグ）／`sam_3_3d`／`tripo_h3_1`
  - `animation_actions` ツールで678種のアニメクリップ検索
  - `autosprite`（等角5方向 idle/walk/run スプライトシート）：**カタログには在るが
    `generate_image` は「Job set type not supported」を返した（実測）**。使うなら
    `apps_search`/`apps_invoke` 経由を試す→駄目なら §4 の代替案で進める（ここで止まらない）。
  - `seedance_2_0`/`veo3_1_lite`（動画）／`upscale_image`（2k/4k）／`remove_background`
- コスト目安：nano_banana_pro 1k×4枚=2cr（実測）。**生成前に必ず `get_cost:true` でプリフライト**。
  3D系は高コスト想定＝1体ずつ検収してから量産。

### コード側の土台（実装済み・壊さない）
- `js/mall_view_3d.js`：一人称3Dレンダラの**実証実装が既にある**（既定OFF・`?mall3d`で切替）。
- `js/mall_rpg.js`：探索/戦闘/ワゴン/エレベーター（M0〜M3改修済み）。敵画像台帳=`RPG_ENEMY_IMG`。
- `js/photo_game.js`＋`js/ui_konron_map.js`：撮影/シャッターチャンス/旅ノート/隠しスポット（T0〜T3済み）。
- `docs/scene_engine_and_mall_redesign.md`：**canvasシーン基盤の設計書が既にある**（2026-06合意・未実装）。
  「歩けるマップ」はこの設計を土台にする（ゼロから設計しない）。
- Live2Dリグ（L2_RIG）と race_canvas のトレース描画＝キャラ表現の既存資産。

---

## 1. 全体像（3フェーズ群・優先順）

| 群 | 内容 | 成果物 |
|---|---|---|
| **G1 モール3D化＋キャラ配置**（必須） | mall_view_3d を既定ONに引き上げ、生成テクスチャ＋敵3D/ビルボードを配置 | 3D迷宮で敵が「そこに居る」 |
| **G2 島めぐりRPG化**（スーファミ品質） | scene_engine 実装＋歩ける崑崙の街＋NPC/スポット интеракション | 十字キーで歩く島・話す・入る |
| **G3 生成アセット横断** | 島地図刷新（§7.1発注済み）・rg_2枚再生成・ミニアニメ・BGM | 画の穴が全部埋まる |

**順序**：G1a（3D土台）→ G1b（キャラ配置）→ G2a（scene_engine＋1エリア縦切り）→ G2b（拡張）→ G3は並走可。
**トークン/クレジット規律**：各生成は 1)get_costプリフライト 2)1点検収 3)量産、の3段階。クレジット残を
フェーズ開始時に `balance` で確認し、200を切ったら生成を止めてユーザーに報告。

---

## 2. G1：モールダンジョンの3D化＋キャラ配置（必須）

### G1a. 3Dビューを主役に引き上げる
1. **現状監査から**（コードだけで判断しない）：`?mall3d` で mall_view_3d を実プレイし、
   「何が足りなくて既定OFFなのか」を箇条書きにしてから設計する（MALL_ADVENTURE_BRIEF の教訓）。
2. 期待される不足＝壁/床が手続き単色で質感がない・敵/宝箱/階段の表現が薄い・性能未検証。
3. **生成テクスチャで質感を入れる**：フロアごとに壁/床/天井のタイルテクスチャ（512×512・シームレス）を
   nano_banana_pro で生成。プロンプト核：
   `seamless tileable texture, {1F tropical beach resort mall / 3F food court lanterns / 5F luxury brass and marble...}, game wall texture, soft painterly, no text`
   フロア雛形は RPG_FLOORS の accent 色に合わせる。webp化して `images/rpg/tex/<floor>_<wall|floor>.webp`。
4. mall_view_3d のレイキャスト描画に**テクスチャサンプリング**を足す（canvas ImageData 縦ストライプ方式・
   race_canvas のトレース描画と同系の手法）。`prefers-reduced-motion`/低速端末は現行の単色にフォールバック。
5. 既定ONへの切替は**性能ゲート**（初回に1秒計測→30fps未満は2Dへ自動フォールバック）付きで。

### G1b. キャラクター配置（敵・店主・ミミ）
1. **敵の3D化**：既存納品画 `images/rpg/en_*.webp`（8枚・意匠確定）を `image_to_3d`
   （should_texture:true・target_polycount:8000〜15000＝スマホ向け低ポリ）でGLB化。
   まず **boss_donryu 1体だけ**生成→検収（形が画と一致するか）→合格したら残りを量産。
2. **表示は2段構え**：
   - 軽量path＝GLBを**8方向プリレンダしたビルボードスプライト**にベイク（オフラインで
     スクショ→透過→`images/rpg/bill/<id>_<dir>.webp`）。3Dビュー内で距離スケール描画。実装が単純で確実。
   - リッチpath（余力があれば）＝three.js等は**入れない**方針を維持（classic script構成）。
     自前レイキャスタにGLBは載らないため、**ビルボードが本線**。GLBは図鑑ビューア（回転閲覧）で活用。
3. **店主スミカの配置**：店の前に店主ビルボード（既存立ち絵を remove_background →縮小）＋
   吹き出し。門番=rpgKeeperMet() 経由（実装済みの敬称ロジックを流用）。
4. **ミミの手**：一人称の画面下にミミの手/耳の前景パーツ（立ち絵から切り出し生成）＝「自分がいる」感。
5. 検収基準：3Dビューで 敵接近→戦闘遷移が違和感なく繋がる・375px実機30fps・絵文字フォールバック健在。

### G1c. 戦闘の3D舞台（任意・G1a/bの後）
- 戦闘背景をフロアテクスチャと同系の生成一枚絵に（`images/rpg/btl/<floor>.webp`）。
  敵はビルボード大写し＝rpgDrawBattle の絵文字/画像描画を差し替え。数値・挙動は一切不変。

---

## 3. G2：島めぐりのスーファミRPG化（歩ける崑崙島）

### 設計原則（リサーチ済みの要点）
- **スーファミRPGの探索の正体**＝タイル移動＋「調べる/話す/入る」の3動詞＋見えない壁のない小さな箱庭。
  A Short Hike（発見が次の発見を開く・失敗なし）とポケモンスナップ（再訪で変化）は撮影ミニゲームで
  実装済み。G2はそれを「歩く」で包む＝**既存の観光/撮影/食事/スカウトが“町の中の建物”になる**。
- **新しい駅は作らない**（GAME_EXPERIENCE_DESIGN §4）：歩けるマップは「島時間」駅の新しい入り方であって、
  既存機能の置き換えではない。従来のピン式マップも残す（設定でどちらでも）。

### G2a. 縦切り（港町・市街エリア1枚を歩ける化）
1. `js/scene_engine.js` を docs/scene_engine_and_mall_redesign.md §1 のAPIスケッチ通りに実装
   （canvas1枚・固定タイムステップ・レイヤ・Actor・オンスクリーン十字キー・遅延ロード）。
2. **背景**：見下ろし俯瞰の町マップ1枚を nano_banana_pro で生成（2k・気持ちは「聖剣伝説の町」）：
   `top-down 3/4 view JRPG town map, tropical volcanic harbor town, stone paths, market stalls,
   lanterns, palm trees, warm colors, SNES RPG style painted look, game map, no text, no UI`
   → 検収（道が繋がっている/建物の入口が読める）→ `images/scene/konron/city.webp`。
   歩行可否は**コリジョンマスク**（同じ構図で `walkable areas as white, buildings and water as black`
   を image-to-image で生成 or 手動で塗る）＝ `city_mask.png` を getImageData 判定。
3. **ミミのスプライト**：4方向×歩行2-3コマ。入手優先順：
   a) `apps_search`/`apps_invoke` で autosprite 相当を探す（§0の実測エラーの回避路）
   b) nano_banana_pro の image-to-image でミミ立ち絵（media_id 再利用可）から
      `chibi pixel sprite sheet, 4 directions walk cycle, 3 frames each, transparent background`
   c) 最終手段＝Live2Dリグ（L2_RIG）を正面のみ流用＋左右反転
   どれでも**必ず1枚検収してから**先へ。`images/scene/konron/mimi_walk.webp`。
4. **インタラクション**：建物入口に到達→既存画面へ（市場→renderMeals・モール→renderMall・
   写真スポット→_kmStartShoot・NPC→到着VNの一言）。**中身は全部既存機能**＝箱庭が導線になる。
5. 入口：観光ハブに「🚶 歩いてまわる（β）」カード。従来のピン式は不変。
6. 検収：375px実機で歩ける・入口が機能する・30fps・リロード後も壊れない・err0。

### G2b. 拡張（G2a合格後）
- エリア追加（レース場→温泉郷→浜）・時間帯パレット（_kmIslandNowと連動）・
  すれちがいNPC（汎用亜人ビルボード数種を生成）・ポロが後ろをついてくる（poroFound後）。
- 隠しスポット（T3実装済み）の実地発見＝マップ上で「？」の煙が立つ、等の演出はここで。

---

## 4. G3：生成アセット横断（並走可）

| # | 対象 | 手順 | 状態 |
|---|---|---|---|
| G3-1 | **島地図刷新** | MINIGAME_LEVELUP_DIRECTIVE **§7.1 に完全ブリーフ済み**（発注済・最優先） | 未着手 |
| G3-2 | rg_lumina/rg_caldera 写真 | 同 §7.1 のC系プロンプト規範・アートバイブル§0.4厳守（山頂湖NG） | 未着手 |
| G3-3 | モール敵スプライト増補 | 同 §7 の表（en_*.webp 画風アンカー・512透過） | 未着手 |
| G3-4 | ミニアニメ | 傑作☆3の瞬間/モール制覇/開花カットインに2-3秒ループ（seedance_2_0 fast 480p→webm）。※音は generate_audio:false | 未着手 |
| G3-5 | BGM3曲(mall-day/boss/fever) | 同 §7。`generate_audio` 系ツールで90秒ループ | 未着手 |
| G3-6 | GLB図鑑ビューア | G1bのGLBを竜/敵図鑑で回転閲覧（`<model-viewer>`は外部CDN不可＝自前の簡易回転ビルボードで代替） | 未着手 |

---

## 5. フェーズ表と完了条件

| # | 内容 | 完了条件（検収） |
|---|---|---|
| **G0** | Higgsfield疎通＋G3-1島地図（§7.1） | balance確認・地図4候補→QA→wire→ピン再調整→実機スクショ→両ブランチ |
| **G1a** | 3Dビュー質感＋既定ON | テクスチャ8種検収・30fpsゲート・2Dフォールバック・実機スクショ |

### 実施ログ（2026-07-27・Opus 5）

**G0＝完了**
- balance 1200cr / Plus。§7.1のプロンプトで1巡目4枚→**3枚NG**（"miniature diorama"が机の上の模型として解釈され部屋/照明が写り込む×2、`No labels`を無視して方角文字が焼き込まれる×1）。
- 2巡目は文言を「ヘリからの実写空撮・模型/机/部屋/照明ではない・文字類を一切出さない」に修正して4枚→**4枚とも構図成立**。うち山頂湖NGを完全に避け（湖＝峰の麓の崩落窪地）、9地区すべてが読める1枚を採用。
- `upscale_image` 4k（4096×2751）→ webp 1.07MB（旧1.54MBより軽い）。旧図は `images/konron/island_map_old.webp` に温存。
- **ピン再調整**（新図をグリッド計測して実測）：city 15,42／falls 31,31／race 36,52／sanctum 50,39／
  onsen 57,60／cliff 86,46／beach 27,78／fishing 82,72／okuchi 72,20。実機で9ピン全てが該当地形の上に載ることと、
  `km-zoom` の9エリア分の切り出しに主役が入ることをスクショで確認。

**G1a＝コード＋テクスチャ完了（G1b以降は未着手）**
- 監査結果：`?mall3d` の実プレイで「黒い部屋＋ネオン線だけ・壁も床も質感ゼロ」＝既定OFFだった理由を確認。
- `js/mall_view_3d.js` にテクスチャ層を追加（`mall_rpg.js` は無改変）。側壁＝画面xから奥行きzを逆算する
  縦ストライプサンプリング、床/天井＝横ストライプでオフスクリーンに焼き込みキャッシュ。
- テクスチャ **18枚納品**（`images/rpg/tex/<slug>_<wall|floor>.webp`・512・計約0.5MB）。
  slug＝beach/pool/gourmet/sea/luxe/depart/fes/sunset/tower。天井は当面ダーク（テクスチャ無しでも成立）。
- **既定ON**。退避口＝`?mall2d` ／ `mallView3D(false)`。
- **30fpsゲートを実測で検証**（合成タイムスタンプで駆動）：60fps相当→3D維持(59.9)／**16.7fps相当→2Dへ自動退避(16.7)**／
  1fps相当（タブ非表示スロットリング）→判定せず3D維持＝誤退避なし。`document.hidden` 中は計測しない。
- 画像が無い/読めない/`prefers-reduced-motion`/低メモリ端末は**従来の単色描画へフォールバック**（壊れない）。
**G1b＝完了（ただし設計は監査で1点変更）**
- ★**監査で判明＝敵は「配置」ではなくランダムエンカウント**（22%/歩・`rpgWalk`）。床に置かれて歩き回る
  敵は存在せず、**敵は必ず正面からしか画面に出ない**。よって §2 G1b-1/2 の「GLB化→8方向ビルボードにベイク」は
  **横向き・後ろ向きが一度も使われない**＝効かない。既存の512透過アート（`RPG_ENEMY_IMG`）をそのまま
  回廊に立てる方式に変更した。GLBは回転閲覧が要る **G3-6（図鑑ビューア）側の道具**として残す。
- **敵の接近シルエット**：`rpgFx.telegraph` の予兆（470ms）に、そのフロアの出現表から選んだ1体を
  **影**として奥(3セル)から手前(0.15セル)へ寄せる。正体は従来どおり突入後の名前カットインで割れる。
  `mall_rpg.js` は無改変＝`mall_view_3d.js` 側で `rpgFx.telegraph` を包んで受け取る。
  - アート持ち＝α保持のまま真っ黒に塗って影に。**アート未納品の敵＝絵文字を影化**（20体中9体しかアートが無い）。
    ※`ctx.filter="brightness(0)"` は灰色の塊になって形が読めなかった（実測）→ オフスクリーン＋`source-atop` に変更。
  - 予兆は470msしかないので、**フロア入場時に出現表のアートを先読み**（初手が絵文字→アートに化けるのを防ぐ）。
- **ミミの前景**：一人称の左下にミミ自身の手＋LEON MALLの紙袋（`images/rpg/fg/mimi_hand.webp`）。
  出典は決裁済みの `leonmall` 立ち絵（§5.5）＝ media_upload → image-to-image（クロマキー緑）→ ローカルで
  キー抜き＋境界クロップ。歩きのゆれ付き（reduced-motion では止める）。画像が無ければ描かない。
- **店主**：監査の結果 **店はフロア単位のボタン式で迷宮に店セルが無い**（「店の前に」は現実と不一致）。
  ショップ画面には既に `.rpg-shopkeep`＋吹き出し＋門番 `rpgKeeperMet()` が実装済みだったので、
  残っていた「顔が絵文字」だけを直した＝**出会った後のみスミカの立ち絵**、出会う前は匿名のまま 💁‍♀️。
  実機で門番の両側を確認（未登場＝店員さん/お客様/絵文字、登場後＝スミカ/ミミ様/立ち絵46px）。
**G2a＝完了（歩ける港町の縦切り）**
- `js/scene_engine.js` を設計書 §1 の API スケッチどおりに新設（canvas1枚／固定タイムステップ1/60＋可変描画／
  レイヤ bgFar…ui／Actorのzソート／キーボード＋オンスクリーン十字キー／宣言的マニフェストの遅延ロード／
  DPR上限2／reduced-motion）。**状態は持たない**＝レース数値へは構造的に触れない。
- `js/scene_konron.js`＝港町・市街1枚。背景 `images/scene/konron/city.webp`（1920×1440・SNES風ペイント調＝決裁2）、
  当たり判定は 32×24 のマス目（背景を10%グリッドで実測して起こし、**赤塗りオーバーレイで突き合わせて是正**）。
- ミミ＝3方向×3コマの歩行シート `images/scene/konron/mimi_walk.webp`。leonmall立ち絵から image-to-image →
  クロマキー抜き → **全コマ共通スケール＋下端そろえ**でシート化（コマごとにトリムすると足が跳ねる）。横向きは反転で右向きも賄う。
- **入口5つ＝中身は全部既存機能**：モール→`renderMall` ／ 市場→`renderMeals` ／ 見晴らし台→`_kmStartShoot("mistra")` ／
  宿→門番つきの一言 ／ 桟橋→`renderKonronMap`。5つとも**歩いて到達できること**をBFS経路＋実移動で確認。
- 入口＝観光ハブの「🚶 歩いてまわる（β）」カード。**従来のピン式は不変**（設計の芯＝新しい駅は作らない）。
- 実機で踏んで直した2点：
  1. **撮影はオーバーレイ**（`pgOpen`）なのにシーンを破棄していた→閉じるとキャンバスの無い抜け殻が残った。留まる方式へ。
  2. 戻るボタン以外（`renderHome` 等）で離脱すると**ループとwindowのキー購読が生き残った**→基盤に掃除係
     （900msごとに `canvas.isConnected` を見て自壊）。rAFはタブ非表示で止まるので判定は setInterval 側に置く。
- 計測：1フレームぶんの更新＋描画が **0.56ms**（DPR2・374×499＝748×998実描画）＝60fps予算16.7msに対して十分。
  ※ブラウザのペインが非表示でrAFが止まる環境のため「実測フレームレート」ではなく**1フレームの処理コスト**。
  入口の道標は絵文字をやめ、ドット絵に馴染む灯り＋下向き矢に（絵文字はHUDのプロンプト側で見せる）。
**G2b＝完了（エリア拡張・時間帯・随行・すれちがい・隠し）**
- **エリア3枚**：港町・市街 ⇄ **聖龍レース場・前庭** ⇄ **ウロコトロ温泉郷**。エリア間は徒歩の道でつながる
  （坂／小径／鳥居）。**エリア移動＝シーンを作り直す**＝設計書 §2「入場でロード／退場で破棄」どおり、
  常にキャンバスは1枚しか生きていない（実機で一巡して確認）。
  当たり判定はエリアごとに32×24を実測（赤塗り合成で背景と突き合わせ）。BFSで**全入口が開始地点から歩いて到達可能**を機械検証。
- **時間帯パレット**：`_kmIslandNow()` の6刻み（未明/朝/昼/夕暮れ/宵/夜）で画面全体を着色。
  夜と宵は multiply、朝夕は source-over＝灯りの窓と提灯が沈まない。HUDにも「🌙 夜」を出して島の一日とそろえた。
- **すれちがいNPC**：亜人の村人6種を1枚のシートに生成（`images/scene/konron/folk.webp`）。
  歩ける範囲を気ままに歩き、🔍 で一言。エリアごとに2〜3人。
- **ポロ随行**：`poroFound()` の後だけ、既存の立ち絵から切り出したポロが少し後ろをついてくる。
  ★足あとは**フレームではなく距離で刻む**（7px毎・13点＝約90px後ろ）。フレームで刻むと
  壁に押しつけている間に同じ座標が溜まり、**ポロがミミに重なった**（実機で踏んで是正）。
- **隠しスポットの「？」**：`KM_HIDDEN` の条件を満たしたときだけ路地に白い「？」が立つ。
  副作用のある `_kmHiddenOk` ではなく**生の cond() を見る**（fail-closed）。条件を落とすと入口ごと消えることを
  逆向きに確認済み（7→6）。
**G3-2／G3-3＝完了。G3-4／G3-5／G3-6 は未了（うちG3-5は道具の制約で不可）**
- **G3-3 敵スプライト＝これで全20種に絵がついた**。`en_cartrat` を画風アンカーに11体を生成
  （selfie/gourmet/stroller/oldies/kid/luxe/hula/influencer/kowako/shisa/kumonosei）。
  白背景を**縁から繋がっている白だけ**抜いて512透過webpに揃え、唯一の台帳 `RPG_ENEMY_IMG` に1行ずつ結線。
  実機で **台帳20件／読めた20件／404は0件／絵の無い敵0体** を確認。
  ★副次効果：G1bの「接近する影」が、絵文字にフォールバックしていた敵でも**アートの影**で出るようになった。
- **G3-2 コース写真**：`rg_lumina`（谷あいに満ちる光・白砂・滝の霧）と `rg_caldera`（黒砂利の周回路・
  割れた溶岩の赤・陽炎、遠景に**尖った活火山の峰＋噴煙**）を撮り直して結線。旧PNGの不一致（明るい光条→
  暗い霧漁村／赤い溶岩→寒色地熱農村）を解消。アートバイブル §0.4 準拠（山頂湖なし・峰は尖った活火山）。
  既存コース写真と同解像度のため upscale は不要と判断（1264×848／既存 grandclock は1280×850）。
- **G3-5 BGM＝この道具では作れない**。`generate_audio` は音声合成専用で、
  「音楽・効果音は一般用途では生成できない／`sonilo_music` はゲーム生成パイプライン専用で
  単独利用してはならない」とツール自身が明示している。既存 `bgm/` と同じく **Suno でユーザー側が作る**のが正道
  （設計書 scene_engine_and_mall_redesign.md §4 も「音楽=Suno」）。ここは着手せず残した。
- **G3-4 ミニアニメ／G3-6 GLB図鑑ビューア＝未着手**。G3-6 は G1b で「回廊にGLBは要らない」と判断して
  GLBを作っていないため、着手するならまず1体生成するところから。
**G3-4 ミニアニメ＝完了**
- `js/miniclip.js` を新設。素材は**真っ黒の背景に光だけ**が動く4秒動画で、`mix-blend-mode: screen` で重ねる
  ＝黒は透け光だけ乗る。アルファ付き動画（webm alpha）の端末差を踏まない作りにした。
- 差し込み先3つ：**傑作☆3**（photo_game）／**屋上制覇**（mall_rpg）／**開花**（field_lessons）。
  いずれも1行の追加で、`window.MiniClip` が無い・素材が無い・読めない・自動再生を拒否された・
  `prefers-reduced-motion` のいずれでも**何も出さずに本編はそのまま**進む。
- ★形式は指示書の webm ではなく **mp4/H.264**。この環境に ffmpeg が無く、かつ **iOS Safari は webm が怪しい**ので
  mp4 の方が実機で確実。`src` はその瞬間が来て初めて入れる＝初回読み込みには乗らない（合計4.8MB／遅延取得）。
- 実機で踏んで直した2点：
  1. **傑作クリップが薄すぎた**（光る画素率がピークでも6%＝ほぼ見えない）→ 「全面を常に光で満たす」方向で作り直し、
     **4秒間ずっと48〜57%** に改善。※シークで測ると黒フレームが返るので、**再生しながら測る**こと。
  2. **連打で3枚重なった**（コメントには「重ならない」と書いてあったのに）→ 新しく出す前に即時撤去する方式に修正し、
     3連打でも1枚だけになることを再確認。
- 開花の素材は**後半にかけて花がほどける**（実測で2.6秒あたりが最大）ため、この1本だけ4秒見せる。
**G3-6 GLB図鑑ビューア＝仕組みは完成。素材はボス1体ぶんだけ（クレジットの都合）**
- まず **ずかんが文字だけだった**のを絵の棚に作り直した（G3-3で全20種に絵がついたのに1枚も出ていなかった）。
  未遭遇は**真っ黒のシルエット＋押せない**＝出会う前に姿を明かさない。押すと回転ビューアが開く。
- `js/dex_view.js`＝**自前の回転ビルボード**（`<model-viewer>` は外部CDNが要るので使えない／指示書どおり）。
  コマは `images/rpg/turn/<id>.webp`（横並び・枚数は幅÷高さで自動判定）。**無ければ1枚絵を大きく見せるだけ**に落ちる
  ＝素材があるものは回り、無いものもちゃんと図鑑として成立する。
- **GLBは実際に作って焼いた**：`image_to_3d`（texture付き・12000ポリ）でボスをGLB化 →
  スクラッチパッドに**使い捨ての最小WebGL2レンダラ**を書いてY回転16コマを透過で焼き、
  `images/rpg/turn/boss1.webp`（3072×192・167KB）として納品。ゲーム本体にレンダラは入れない（配信されるのは焼いた画像だけ）。
  - ★焼き込みの落とし穴：**行列を自前で組むと簡単に空になる**（1回全滅した）。
    中心・スケール・角度を uniform で渡して**頂点シェーダ内で回す**と確実。UINT32インデックスもあるのでWebGL2で。
    保存前に「透過でない画素が何%あるか」を数えて、空なら保存しないようにした。
- ★**残り18体ぶんは作っていない**：`image_to_3d` は **1体30クレジット**で、20体だと600＝G3の枠(300)を超える。
  仕組みは全部通っているので、やるなら1体ずつ足すだけ（`images/rpg/turn/<id>.webp` を置けば勝手に回り出す）。
- 実機で踏んで直した2点（どちらも「rAFに頼りすぎ」）：
  1. 開いた直後にキャンバスが空のまま（タブが裏だと rAF が止まるため）→ **開いた瞬間と素材到着時に必ず1枚描く**。
  2. ドラッグしても回らない（値だけ動いて再描画されない）→ **指の動きの中で描く**。
**G1c 戦闘の3D舞台＝完了**
- フロア別の生成一枚絵 `images/rpg/btl/<slug>.webp` を9枚納品（beach/pool/gourmet/sea/luxe/depart/fes/sunset/tower・
  計370KB）。**slugは3Dビューのテクスチャと共通**＝回廊で見た景色のまま戦場に入る。
  上1/3〜2/3に見せ場、下1/3は空にして描かせた＝アイソメの舞台がそのまま置ける。
- `rpgDrawBattle` は**背景の差し替えだけ**。舞台・キャラ・演出・数値・挙動はそのまま
  （画像が無ければ従来の手続き背景に落ちる＝404も出ない）。
- ★**敵をビルボードの大写しに**：観光客は今まで**必ず手続きスプライト**で描かれていた（G3-3以前は絵が無かったため）。
  全20種に絵が揃ったので「絵があれば絵」を優先に変更し、サイズも 48/70 → 84/104 へ。
  絵が無い時だけ従来の手続きスプライト／絵文字に落ちる。
- 実機で1F/3F/5F/屋上×別々の敵を描き分けて確認。戦闘を1手すすめて**ダメージは入り所持金は動かない**ことも確認。err0。
**天井テクスチャ＝完了**（G1aの積み残し）
- 9枚納品（`images/rpg/tex/<slug>_ceil.webp`）。壁/床と同じ slug ＝ フロアごとに天井まで表情が変わる。
  ビーチ=白木＋ラタン／プール=空／グルメ=提灯の梁／海竜=水槽のガラス／ラグジュアリー=大理石の格天井／
  百貨店=紫と金／フェス=万国旗と提灯／屋上=夕焼け雲／タワー=蒼いヴォールト。
- コードはG1aの時点で `<slug>_ceil` を見に行くようになっていたので、**置いただけで載った**（結線の変更なし）。
- ※検証中に「壁が出ない」と一度見えたが、これは**テスト側が画像の読み込みを待たずに撮っていた**だけで、
  実装のフォールバック（未ロード＝単色）が正しく働いていた。先読みしてから撮り直して全フロア確認済み。

**浜エリア（G2bの4エリア目）＝完了**
- `images/scene/konron/beach.webp`＝セナ浜（白砂・ラグーン・桟橋・茅葺きの小屋・かき氷の屋台）。
  当たり判定32×24を実測（礁と水は入れない／桟橋だけ海に出られる）。到達271マス。
- 入口4つ：桟橋の先→`kwShoot("sena")`／かき氷の屋台→`renderMeals`／温泉郷へもどる道／島の地図へ。
  **温泉郷側にも「浜へくだる道」を追加**して、港町⇄レース場⇄温泉郷⇄浜が徒歩でひとつながりになった。
- 4エリアすべてで「開始地点から全入口へ歩いて到達できる」ことをBFSで機械検証（ALL GREEN）。

**G3-5 BGM＝発注書を用意**（`docs/BGM_SUNO_BRIEF.md`）。Sunoにそのまま貼れるStyleプロンプト3本＋
ファイル名（`bgm/mallbgm/mall-day|mall-boss|mall-fever.mp3`）＋長さ90秒ループ・インスト指定。
※日本語ファイル名は過去にNFDで404事故があるため半角英数を指定。曲が届いたら結線はこちらでやる。

- 未了：残り18体のGLB（30cr/体）。BGM3曲（ユーザーがSunoで生成 → こちらで結線）。
| **G1b** | キャラ配置 | boss_donryu GLB検収→敵8方向ビルボード→店主/ミミ前景・実機スクショ |
| **G2a** | 歩ける港町（縦切り） | scene_engine＋city1枚＋ミミ4方向＋入口5つ機能・実機動画/スクショ |
| **G2b** | エリア拡張 | +2エリア・時間帯・ポロ随行 |
| **G3-x** | 横断アセット | 各行の検収列 |

**毎フェーズ共通**：編集→grep実体確認→構文→実機→ ?v バンプ（perl）→ 両ブランチ → **git log で実在確認**
→ メモリ更新。「たぶん動く」禁止。ツール出力が読めなくなったら**即座に作業を止めてユーザーに報告**
（幻覚事故の再発防止）。

---

## 5.5 ミミの衣装指定（ユーザー決裁済み 2026-07-26・スプライト/前景生成の前提）

**ミニゲーム中のミミは固定衣装**（プレイヤーの着用衣装に追従させない＝全衣装差分は作らない）：
- **モールで大冒険** → `leonmall`「モールでお買い物」（`images/cast/mimi/mimi_leonmall_default.png` 実在）
- **ドラゴンスカウト** → `tarzan`「野生児ターザン」（`images/cast/mimi/mimi_tarzan_default.png` 実在）
- G1bのミミ前景・G2aの歩行スプライト・スカウト画面のミミ表現は、この2枚を生成の source にする
  （media_upload→スプライト化。buniqroのアップロード済みmedia_idは island 汎用に流用可）。
- 島めぐり（G2）の歩行ミミは既定衣装系でよい（指定なし＝buniqro基準）。

## 5.6 レースビジュアルの刷新（ユーザー発注 2026-07-26・刷新できるところから）

レース数値・タイムライン・FinalPowerは**完全不変**のまま、見た目レイヤーだけを順に格上げする：
1. **コース背景**：rg_* 6枚（T0結線済み）と同じ実写級規範で、レース画面の背景帯
   （race_canvas の下30%走路ルール＝[[match-field-stadium-blend]]の中央空け規範）を張り替え候補に。
   既存 images/racebg/ を監査→見劣りする面から §7.1 と同じ QA 手順で再生成。
2. **観客・旗・紙吹雪のミニアニメ**：seedance 2-3秒ループ→webm→発走/ゴールの演出層に合成（表示専用）。
3. **竜の表現**：HD-2Dドット竜52頭は確定画風（[[dragon-visual-overhaul-hd2d]]・別生成禁止）＝**触らない**。
   刷新対象は背景・演出・UIフレームのみ。
4. 検収は毎回「1枚生成→実機で走らせて中央の可読性確認→採用」。一括張り替え禁止。

## 6. ユーザー決裁（2026-07-26 回答済み・確定）

1. **3Dビュー既定ON＝OK**。ただし30fps計測ゲートで未満なら自動的に2Dへフォールバックすること（必須）。
2. **G2の画風＝SNES風ペイント調**（聖剣伝説の町・見下ろし俯瞰・軽い）。実写級ミニチュア調は不採用。
3. **クレジット配分＝指示書の目安どおり**：G0=10・G1=300・G2=200・G3=300（残りは予備）。
※衣装指定（§5.5）とレースビジュアル刷新（§5.6）も決裁済み。
